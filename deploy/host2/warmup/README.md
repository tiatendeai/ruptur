# Warmup Manager Deployment
This directory contains the production-ready bundle and Docker configuration for the Warmup Manager.

## Runtime
- Serves assets from `dist/`
- Exposes API at `/api/local`
- Managed by `runtime/server.mjs`
